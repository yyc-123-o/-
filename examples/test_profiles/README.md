# 三档个性化演示画像

这三份文件使用同一目标章节 `ch03_cnn` 和同一画像 Agent v2.1 格式，只改变学习者状态：

- `profile_low.json`：基础薄弱、概念混淆，CNN 入门；
- `profile_mid.json`：数学基础较好、代码能力偏弱，CNN 巩固；
- `profile_high.json`：前置知识和 CNN 已掌握，CNN 跳过，转向后续薄弱点。

在前端依次上传三份文件，可以对比推荐队列、资源支架和测评后的调整记录。
